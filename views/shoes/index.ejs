<%- include('../partials/html-head') %>
<%- include('../partials/nav') %>

<section>
  <h1>Kickz! 👟</h1>
  <% if (user) { %>
    <h2>Add a pair of kickz to your collection!</h2>
    <form action="/shoes" method="POST">
      <label>
        Brand: 
        <input type="text" name="brand" autocomplete="off">
      </label>
      <label>
        Name:
        <input type="text" name="name" autocomplete="off">
      </label>
      <label>
        Color:
        <input type="text" name="color" autocomplete="off">
      </label>
      <button type="submit">Add 👟</button>
    </form>
  <% } %>
  <section>
    <h1>All Kickz</h1>
    <ul>
    <% shoes.forEach(shoe => { %>
      <li>
        👟
        <a href="/shoes/<%= shoe._id %>"><%= shoe.brand %> <%= shoe.name %></a> - <%= shoe.color %>
      </li>
    <% }) %>
    </ul>
  </section>
</section>